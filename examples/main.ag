let
   a =  "cds";
   b =  "cds";
in
{
    port = """
    SELECT *
    FROM student s
    WHERE s.Id = {}
    """;

    routes = [
       {
           path = "/";
           handler = () => html("<h1>Hello</h1>");
       }
    ];


    middleware = [
       () => {},
       () => {},
       () => {},
       () => {},
       () => {},
       () => {},
       () => {},
    ];



}
